import { Handler } from '@netlify/functions';

const handler: Handler = async (event, context) => {
  // Aquí simulas los datos que antes venían de tu base de datos
  const leaderboardData = [
    { id: 1, name: "Usuario Pro", score: 1500 },
    { id: 2, name: "Gamer123", score: 1200 },
    { id: 3, name: "Novato99", score: 800 }
  ];

  return {
    statusCode: 200,
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(leaderboardData),
  };
};

export { handler };
[build]
  command = "npm run build"
  publish = "dist" # O "build", según uses Vite o React
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200